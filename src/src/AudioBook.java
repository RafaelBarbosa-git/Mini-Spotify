import java.time.Duration;

public class AudioBook extends Midias{
    public AudioBook(String titulo, String artista, Duration duracao, Genero genero) {
        super(titulo, artista, duracao, genero);
    }
}
